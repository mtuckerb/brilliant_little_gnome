<%= erb :'partials/course_detail_header' %>
<div class="columns">
  <div class="column is-3">
    <%= erb :'partials/sidebar' %>
  </div>
  <div class="column is-9">
    <div class="box">
      <div class="level">
        <div class="level-left">
          <h4 class="title is-4"><i class="fas fa-tasks mr-2"></i> Assignments</h4>
        </div>
        <div class="level-right">
          <a href="/course/<%= @course_id %>/assignments/export.ics" class="button is-small is-primary is-outlined">
            <span class="icon is-small"><i class="fas fa-calendar-alt"></i></span>
            <span>Export to Calendar</span>
          </a>
        </div>
      </div>
      <% if @assignments && !@assignments.empty? %>
        <table class="table is-fullwidth is-hoverable">
          <thead>
            <tr>
              <th>Assignment</th>
              <th class="has-text-right">Action</th>
            </tr>
          </thead>
          <tbody>
            <% @assignments.each do |a| %>
              <tr>
                <td>
                  <div class="mb-1">
                    <strong><a href="/course/<%= @course_id %>/assignments/<%= a['Id'] %>"><%= a['Name'] %></a></strong>
                  </div>
                  <div class="is-size-7 has-text-grey">
                    <i class="fas fa-calendar-alt mr-1"></i> Due: <%= a['DueDate'] ? Time.parse(a['DueDate']).strftime("%B %d, %Y %I:%M %p") : "No due date" %>
                  </div>
                </td>
                <td class="has-text-right">
                  <a href="/course/<%= @course_id %>/assignments/<%= a['Id'] %>" class="button is-small is-primary is-light">View Details</a>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p class="has-text-grey">No assignments found for this course.</p>
      <% end %>
    </div>
  </div>
</div>
