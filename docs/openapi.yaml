openapi: 3.0.0
info:
  title: Brilliant Sidecar API
  version: 1.4.0
  description: |
    REST API for interacting with cached LMS data and Model Context Protocol (MCP) server integration.
    
    All `/api/v1/*` endpoints require authentication via:
    1. `X-API-KEY` header
    2. Bearer JWT Token in `Authorization` header
    3. Active internal session (for browser-based SPA)

servers:
  - url: /api/v1
    description: Local API server

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-KEY
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Course:
      type: object
      properties:
        id: { type: integer }
        org_unit_id: { type: string }
        name: { type: string }
        code: { type: string }
        semester: { type: string }
        is_pinned: { type: boolean }
        last_accessed_at: { type: string, format: date-time }
        banner_url: { type: string }

    Notification:
      type: object
      properties:
        id: { type: integer }
        external_id: { type: string }
        title: { type: string }
        body: { type: string }
        date: { type: string, format: date-time }
        course_id: { type: string }
        course_name: { type: string }
        semester: { type: string }
        urgency: { type: integer, description: "1 (Low) to 5 (High)" }
        is_personal: { type: boolean }
        is_read: { type: boolean }
        url: { type: string }

    Grade:
      type: object
      properties:
        id: { type: integer }
        brightspace_id: { type: string }
        name: { type: string }
        displayed_grade: { type: string }
        numerator: { type: number }
        denominator: { type: number }
        weight: { type: number }
        due_date: { type: string, format: date-time }
        comments: { type: string }

    Preferences:
      type: object
      properties:
        display_name: { type: string }
        time_zone: { type: string }
        historic_gpa: { type: number }
        historic_units: { type: integer }
        default_semester: { type: string }

security:
  - ApiKeyAuth: []
  - BearerAuth: []

paths:
  /status:
    get:
      summary: Check system and authentication status
      responses:
        '200':
          description: System status
          content:
            application/json:
              schema:
                type: object
                properties:
                  authenticated: { type: boolean }
                  degraded_mode: { type: boolean }
                  host: { type: string }
                  sync_status: { type: object }

  /courses:
    get:
      summary: List all courses
      description: Returns localized course data. MCP tool `list_courses` provides additional property-level filtering.
      responses:
        '200':
          description: List of courses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Course'

  /dashboard/summary:
    get:
      summary: Get consolidated data for the landing dashboard
      responses:
        '200':
          description: Summary of overall GPA, upcoming tasks, and recent activity
          content:
            application/json:
              schema:
                type: object

  /courses/{id}/summary:
    get:
      summary: Get course overview and upcoming items
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Consolidated course data including syllabus and grades
          content:
            application/json:
              schema:
                type: object

  /courses/{id}/announcements:
    get:
      summary: Get course announcements
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: List of announcements with rendered bodies
          content:
            application/json:
              schema:
                type: array

  /courses/{id}/search:
    get:
      summary: Search course content (modules and topics)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: q
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Search results grouped by type
          content:
            application/json:
              schema:
                type: object

  /calendar:
    get:
      summary: Get items for calendar visualization
      parameters:
        - name: view
          in: query
          schema: { type: string, enum: [month, week] }
        - name: date
          in: query
          schema: { type: string, format: date }
        - name: show_completed
          in: query
          schema: { type: boolean }
      responses:
        '200':
          description: Grouped items with visualization metadata
          content:
            application/json:
              schema:
                type: object

  /courses/{id}/grades:
    get:
      summary: Get course grades
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: List of grade entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Grade'

  /notifications:
    get:
      summary: Retrieve notifications with filtering
      parameters:
        - name: course_id
          in: query
          schema: { type: string }
        - name: semester
          in: query
          schema: { type: string }
        - name: urgency
          in: query
          schema: { type: integer }
        - name: is_personal
          in: query
          schema: { type: boolean }
        - name: show_read
          in: query
          description: Include read notifications (default false)
          schema: { type: boolean }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
      responses:
        '200':
          description: List of notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'

  /preferences:
    get:
      summary: Retrieve current preferences
      responses:
        '200':
          description: User settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Preferences'
    patch:
      summary: Update user settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Preferences'
      responses:
        '200':
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Preferences'
        '422':
          description: Validation error

  /token:
    get:
      summary: Generate a short-lived JWT token for external API access
      responses:
        '200':
          description: A JWT Bearer token
          content:
            application/json:
              schema:
                type: object
                properties:
                  token: { type: string }

  /mcp/sse:
    get:
      summary: MCP SSE Connection Endpoint
      description: Establishes a Server-Sent Events stream for Model Context Protocol.
      security:
        - ApiKeyAuth: []
      responses:
        '200':
          description: SSE Stream established

  /mcp/messages:
    post:
      summary: MCP JSON-RPC Message Handler
      parameters:
        - name: session_id
          in: query
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                jsonrpc: { type: string, example: "2.0" }
                id: { type: string }
                method: { type: string }
                params: { type: object }
      responses:
        '202':
          description: Message accepted for processing
        '200':
          description: Direct synchronous response
