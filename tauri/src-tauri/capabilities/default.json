{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. Custom commands registered via invoke_handler don't need capabilities; this grants the core webview/event APIs the React code uses (listen, emit, invoke).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
